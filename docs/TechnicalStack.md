Here it List the Technical Stack usesd with in this project.


| Component     | Technology                                             | Why                                  |
| ------------- | ------------------------------------------------------ | ------------------------------------ |
| Java          | Java 21 LTS                                            | Latest LTS, long-term support        |
| Framework     | Spring Boot 3.5.x (latest stable at the time we start) | Current generation                   |
| Build Tool    | Maven                                                  | Most common in enterprise Java       |
| ORM           | Spring Data JPA + Hibernate                            | Industry standard                    |
| Migration     | Flyway                                                 | Version-controlled schema management |
| Security      | Spring Security + JWT                                  | Authentication                       |
| Validation    | Jakarta Bean Validation                                | Request validation                   |
| Mapping       | MapStruct                                              | DTO ↔ Entity mapping                 |
| Boilerplate   | Lombok                                                 | Reduces boilerplate                  |
| Database      | MySQL 8                                                | Relational data                      |
| Testing       | JUnit 5 + Mockito + Testcontainers                     | Modern testing                       |
| Documentation | OpenAPI (Swagger)                                      | API documentation                    |
| Configuration | application.yml                                        | Easier to manage than properties     |
| Logging       | SLF4J + Logback                                        | Standard logging                     |


Each Microservice Initialization steps

Lets get Authe service as a example and list down it. This is similar for every microservices with in this project.

auth-service
│
├── Phase A
│     Project Initialization
│
├── Phase B
│     Flyway Setup
│
├── Phase C
│     Database Migration Scripts
│
├── Phase D
│     JPA Entities
│
├── Phase E
│     Repository Layer
│
├── Phase F
│     Service Layer
│
├── Phase G
│     JWT Authentication
│
├── Phase H
│     REST APIs
│
├── Phase I
│     Dockerization
│
└── Phase J
      Integration Testing


### Introduced some Platform Architecture rather than just a Multi-Module Maven Project ####

1. Parent maven project (common Module)
   - contain only truly shared components.
   It should include:
        BaseAuditEntity
        UUID utilities (if we decide to centralize them)
        Common exception hierarchy
        Standard API response model
        Error response model
        Global constants
        Validation utilities
        Shared DTOs (only if they are genuinely cross-cutting)
        Logging utilities
        Common configuration classes
    
    BaseAuditEntity
        Instead of repeating this in every entity:

            createdAt
            createdBy
            updatedAt
            updatedBy
            version

        We'll create one abstract base class.

        Every entity will extend

    UUID Strategy

        I also agree with using UUIDs generated by the application rather than database-generated IDs.

##### Centralized Code Quality #####

In the parent POM we can configure:

    Checkstyle
    SpotBugs
    JaCoCo
    Maven Enforcer Plugin





